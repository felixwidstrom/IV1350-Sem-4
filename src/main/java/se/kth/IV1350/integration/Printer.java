package se.kth.IV1350.integration;

/**
 * A class responsible for simulating the external printer.
 */
public class Printer {
    /**
     * Method for simulating the printing of a receipt through an external printer. (Currently does nothing).
     * @param receipt a string representing a receipt to be printed.
     */
    public void print(String receipt) {
        //Prints the receipt through the external printer
    }
}
