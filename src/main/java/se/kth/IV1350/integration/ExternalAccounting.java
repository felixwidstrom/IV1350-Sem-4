package se.kth.IV1350.integration;

/**
 * A class responsible for simulating the external accounting system.
 */
public class ExternalAccounting {
    /**
     * Method for updating the external accounting. (Currently does nothing).
     * @param totalAmount a double representing the sale total amount.
     */
    public void update(double totalAmount) {
        //Update accounting to account for sale total price.
    }
}
